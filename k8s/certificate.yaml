apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fcs-identity-tls
  namespace: fcs-identity
spec:
  secretName: fcs-identity-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
  dnsNames:
    - fcs-identity.flaviojcf.com.br
